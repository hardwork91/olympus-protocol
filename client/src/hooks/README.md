# hooks/

Custom React hooks reutilizables.

Ejemplos previstos:

- `useRoom(roomId)` — suscribe al listener de Firebase y devuelve el state.
- `useSound()` — wrapper de Howler para SFX.
- `useLocalSeat()` — identifica si soy player 1 o 2 en la sala actual.
